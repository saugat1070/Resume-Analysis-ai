version: '3.9'

services:
  app:
    ports:
      - "3000:3000"
    build: .
    env_file:
      - .env.local
    depends_on:
      - mongo
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    